
import { ActionsTypes } from '../../actions/Swagger/types';

const initialState = { 

};

const SwaggerReducer = (state = initialState, action) => {
  switch (action.type) {
       
    default:
      return state;
  }
};

export default SwaggerReducer;
