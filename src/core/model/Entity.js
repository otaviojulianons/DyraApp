//import { formatarData } from '../../../helpers/Formatters';

class Entity{
  constructor({
    id, 
    name, 
    attributes
  }) {
    this.id = id;
    this.name = name;
    this.attributes = attributes;
  }

}
export default Entity;

