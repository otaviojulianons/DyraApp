//import { formatarData } from '../../../helpers/Formatters';

class DataType{
  constructor({
    id, 
    name, 
    useLength
  }) {
    this.id = id;
    this.name = name;
    this.useLength = useLength;
  }

}
export default DataType;

